name: Run responsive redesign (disabled)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy responsive transformer disabled."
