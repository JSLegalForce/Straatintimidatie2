name: Responsive redesign v2 (disabled)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy responsive transformer disabled. Layout is maintained explicitly in the course preview."
